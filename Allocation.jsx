import { useEffect, useState, useCallback, useMemo } from 'react'; import api, { getErrorMessage } from '../services/api'; import AllocationTable from '../components/AllocationTable'; import BoothWiseAllocation from '../components/BoothWiseAllocation'; import ReallocateModal from '../components/ReallocateModal'; import StatCard from '../components/StatCard'; import Spinner from '../components/Spinner'; import Toast from '../components/Toast'; import ConfirmModal from '../components/ConfirmModal'; import { docDownload } from '../services/download';  const TABS = [   { id: 'allocated', label: 'Allocated Officers' },   { id: 'unallocated', label: 'Unallocated Officers' },   { id: 'boothwise', label: 'Booth-wise Allocation' },   { id: 'history', label: 'Allocation History' }, ];  function successRate(allocated, totalOfficers) {   if (!totalOfficers) return '0.00%';   return `${((allocated / totalOfficers) * 100).toFixed(2)}%`; }  export default function Allocation() {   const [loading, setLoading] = useState(true);   const [loadingStage, setLoadingStage] = useState('Loading uploaded officers???');   const [running, setRunning] = useState(false);   const [runLabel, setRunLabel] = useState('');   const [notify, setNotify] = useState(null);    const [stats, setStats] = useState(null);   const [officers, setOfficers] = useState([]);   const [booths, setBooths] = useState([]);   const [allocations, setAllocations] = useState([]);   const [runResult, setRunResult] = useState(null);    const [tab, setTab] = useState('allocated');   const [historySearch, setHistorySearch] = useState('');   const [historyStatus, setHistoryStatus] = useState('');    const [confirmRun, setConfirmRun] = useState(false);   const [cancelTarget, setCancelTarget] = useState(null);   const [cancelling, setCancelling] = useState(false);   const [reallocTarget, setReallocTarget] = useState(null);   const [sendingIds, setSendingIds] = useState(new Set());    const allocatedList = useMemo(     () => allocations.filter((a) => a.status === 'ALLOCATED'),     [allocations]   );    // Officers with no active ALLOCATED allocation (requirement 11: never hide them).   const unallocatedList = useMemo(() => {     const allocatedOfficerIds = new Set(       allocatedList.map((a) => String(a.officer?._id || a.officer))     );     const reasonById = new Map(       (runResult?.unallocatedOfficers || []).map((u) => [         String(u.officerId || u._id || u.officer?._id),         u.reason,       ])     );     return officers       .filter((o) => !allocatedOfficerIds.has(String(o._id)))       .map((o) => ({         ...o,         unallocatedReason:           reasonById.get(String(o.officerId)) ||           reasonById.get(String(o._id)) ||           'No suitable booth available in the same Mandal.',       }));   }, [officers, allocatedList, runResult]);    const allocationsByBooth = useMemo(() => {     const map = {};     allocatedList.forEach((a) => {       const key = String(a.booth?._id || a.booth);       if (!map[key]) map[key] = [];       map[key].push(a);     });     return map;   }, [allocatedList]);    const historyList = useMemo(() => {     const q = historySearch.trim().toLowerCase();     return allocations.filter((a) => {       if (historyStatus && a.status !== historyStatus) return false;       if (!q) return true;       const hay = [         a.officer?.officerId,         a.officer?.officerName,         a.officer?.mobileNumber,         a.booth?.boothNumber,         a.booth?.boothName,         a.mandal,         a.status,       ]         .filter(Boolean)         .join(' ')         .toLowerCase();       return hay.includes(q);     });   }, [allocations, historySearch, historyStatus]);    const totalRequiredSlots = useMemo(     () => booths.reduce((sum, b) => sum + (b.requiredOfficers || 0), 0),     [booths]   );    const fetchAll = useCallback(async (stageLabel) => {     setLoading(true);     setLoadingStage(stageLabel || 'Loading uploaded officers???');     try {       setLoadingStage('Loading uploaded officers???');       const officersRes = await api.get('/api/officers', { params: { limit: 500 } });       setOfficers(officersRes.data.data || []);       setLoadingStage('Loading uploaded booths???');       const boothsRes = await api.get('/api/booths', { params: { limit: 500 } });       setBooths(boothsRes.data.data || []);       setLoadingStage('Loading existing allocations???');       const [allocRes, statsRes] = await Promise.all([         api.get('/api/allocation', { params: { limit: 500 } }),         api.get('/api/dashboard/stats'),       ]);       setAllocations(allocRes.data.data || []);       setStats(statsRes.data.data || null);     } catch (err) {       setNotify({ message: getErrorMessage(err), type: 'error' });     } finally {       setLoading(false);     }   }, []);    useEffect(() => {     fetchAll().catch(() => {});   }, [fetchAll]);    // POST /api/allocation/run - backend performs the REAL allocation (req. 10).   const doRun = async () => {     setRunning(true);     setConfirmRun(false);     setRunLabel('Running allocation???');     try {       const { data } = await api.post('/api/allocation/run');       setRunLabel('Saving allocation results???');       setRunResult(data.data || null);       setNotify({         message:           `Allocation completed: ${data.data?.allocated ?? 0} allocated, ` +           `${data.data?.unallocated ?? 0} unallocated, ${data.data?.skipped ?? 0} already allocated.`,         type: 'success',         duration: 8000,       });       setTab('allocated');       await fetchAll('Refreshing allocation results???');     } catch (err) {       setNotify({ message: getErrorMessage(err), type: 'error', duration: 8000 });       setRunning(false);       return;     } finally {       setRunning(false);       setRunLabel('');     }   };    // POST /api/allocation/:id/cancel (requirement 17).   const doCancel = async () => {     if (!cancelTarget) return;     setCancelling(true);     try {       await api.post(`/api/allocation/${cancelTarget._id}/cancel`);       setNotify({         message: `Allocation cancelled - officer ${cancelTarget.officer?.officerName || ''} is now unallocated.`,         type: 'success',       });       setCancelTarget(null);       await fetchAll('Refreshing allocation data???');     } catch (err) {       setNotify({ message: getErrorMessage(err), type: 'error', duration: 8000 });       setCancelTarget(null);     } finally {       setCancelling(false);     }   };    // POST /api/notifications/send/:allocationId (requirement H).   const doSendNotification = async (allocation) => {     setSendingIds((prev) => new Set(prev).add(allocation._id));     try {       const { data } = await api.post(`/api/notifications/send/${allocation._id}`);       setNotify({         message: data.message || 'Notification saved with status SENT.',         type: 'success',       });     } catch (err) {       setNotify({ message: getErrorMessage(err), type: 'error', duration: 8000 });     } finally {       setSendingIds((prev) => {         const next = new Set(prev);         next.delete(allocation._id);         return next;       });     }   };    if (loading) {     return (       <div className="page">         <Spinner label={loadingStage || 'Loading allocation data???'} />       </div>     );   }     return (       <div className="page">         <Spinner label={loadingStage || 'Loading allocation data???'} />       </div>     );   }    return (     <div className="page">       {notify ? <Toast {...notify} onClose={() => setNotify(null)} /> : null}       {running ? <Spinner label={runLabel || 'Running allocation???'} /> : null}        <div className="page-head">         <div>           <h1 className="page-title">Allocation</h1>           <p className="page-subtitle">             Automatic booth allocation built from the uploaded Excel data           </p>         </div>         <div className="btn-row">           <button             type="button"             className="btn btn-primary"             disabled={running}             onClick={() => setConfirmRun(true)}           >             {running ? 'Running???' : '??? Run Automatic Allocation'}           </button>           <button             type="button"             className="btn btn-ghost"             disabled={running}             onClick={() => fetchAll('Refreshing allocation data???')}           >             ??? Refresh Data           </button>           <button             type="button"             className="btn btn-ghost"             onClick={() => docDownload('/api/reports/allocation-excel')}           >             ??? Allocated (.xlsx)           </button>         </div>       </div>        {/* ------------------------- Summary cards (req. 7/14) ------------------------- */}       <div className="stat-grid">         <StatCard label="Total Officers" value={stats?.totalOfficers ?? officers.length} icon="????" tone="navy" />         <StatCard label="Total Booths" value={stats?.totalBooths ?? booths.length} icon="???????" tone="blue" />         <StatCard label="Allocated Officers" value={allocatedList.length} icon="???" tone="green" />         <StatCard label="Unallocated Officers" value={unallocatedList.length} icon="??????" tone="red" />         <StatCard label="Required Officer Slots" value={totalRequiredSlots} icon="????" tone="purple" />         <StatCard           label="Available Booth Slots"           value={Math.max(0, totalRequiredSlots - allocatedList.length)}           icon="????"           tone="amber"         />       </div>        {/* ---------------------- Run result summary (req. 14) ---------------------- */}       {runResult ? (         <div className="card">           <h3 className="card-title">Allocation Summary</h3>           <div className="summary-grid">             <div><span className="muted">Total Officers:</span> <strong>{runResult.totalOfficers ?? 0}</strong></div>             <div><span className="muted">Successfully Allocated:</span> <strong>{runResult.allocated ?? 0}</strong></div>             <div><span className="muted">Unallocated:</span> <strong>{runResult.unallocated ?? 0}</strong></div>             <div><span className="muted">Already Allocated (skipped):</span> <strong>{runResult.skipped ?? 0}</strong></div>             <div><span className="muted">Total Booths:</span> <strong>{runResult.totalBooths ?? 0}</strong></div>             <div><span className="muted">Filled Slots:</span> <strong>{allocatedList.length}</strong></div>             <div><span className="muted">Available Slots:</span> <strong>{Math.max(0, totalRequiredSlots - allocatedList.length)}</strong></div>             <div><span className="muted">Cancelled Allocations:</span> <strong>{stats?.cancelledAllocations ?? 0}</strong></div>             <div>               <span className="muted">Success Rate:</span>{' '}               <strong>{successRate(runResult.allocated ?? 0, runResult.totalOfficers ?? 0)}</strong>             </div>           </div>         </div>       ) : null}        {/* ------------------------------ Tabs ------------------------------ */}       <div className="tab-row">         {TABS.map((t) => (           <button             key={t.id}             type="button"             className={`tab-btn ${tab === t.id ? 'active' : ''}`}             onClick={() => setTab(t.id)}           >             {t.label}             {t.id === 'allocated' ? ` (${allocatedList.length})` : ''}             {t.id === 'unallocated' ? ` (${unallocatedList.length})` : ''}           </button>         ))}       </div>          next.delete(allocation._id);         return next;       });     }   }; 